name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: Format, lint, typecheck, test, and build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build

  deploy:
    name: Deploy to Cloudflare Workers
    runs-on: ubuntu-latest
    needs: ci
    if: >
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    concurrency:
      group: sombreado-cloudflare-deploy
      cancel-in-progress: false
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
      ALLOW_SKIP_DEPLOY: ${{ secrets.ALLOW_SKIP_DEPLOY }}

    steps:
      - name: Require Cloudflare deploy secrets on main
        id: secrets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [ -z "${CLOUDFLARE_API_TOKEN}" ] || [ -z "${CLOUDFLARE_ACCOUNT_ID}" ] || [ -z "${NEXT_PUBLIC_API_URL}" ]; then
            if [ "${ALLOW_SKIP_DEPLOY}" = "1" ]; then
              echo "::warning::Cloudflare deploy skipped (ALLOW_SKIP_DEPLOY=1; secrets or vars incomplete)."
              echo "should_deploy=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, and NEXT_PUBLIC_API_URL are required on main."
            echo "Set the Cloudflare token/account as repository secrets, NEXT_PUBLIC_API_URL as a repository variable, or set ALLOW_SKIP_DEPLOY=1 to opt out explicitly."
            exit 1
          fi
          echo "should_deploy=true" >> "$GITHUB_OUTPUT"

      - name: Check out repository
        if: steps.secrets.outputs.should_deploy == 'true'
        uses: actions/checkout@v4

      - name: Set up Node
        if: steps.secrets.outputs.should_deploy == 'true'
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        if: steps.secrets.outputs.should_deploy == 'true'
        run: npm ci

      - name: Build
        if: steps.secrets.outputs.should_deploy == 'true'
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ env.NEXT_PUBLIC_API_URL }}

      - name: Deploy Worker
        id: deploy
        if: steps.secrets.outputs.should_deploy == 'true'
        uses: cloudflare/wrangler-action@da0e0dfe58b7a431659754fdf3f186c529afbe65 # v3.14.1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

      - name: Smoke-check Worker
        if: steps.secrets.outputs.should_deploy == 'true'
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        run: |
          test -n "$DEPLOYMENT_URL"
          curl -fsS --retry 5 --retry-delay 2 "$DEPLOYMENT_URL" -o /dev/null
